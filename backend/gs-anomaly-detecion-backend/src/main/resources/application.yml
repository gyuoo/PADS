server:
  port: 8081
spring:
  application:
    name: dashboard
  profiles:
    active: dev
  jpa:
    open-in-view: false

mybatis:
  mapper-locations: mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true